# Инструкция для ИИ: как добавлять дни со словами в тренажёр «5 mots par jour»

Тренажёр — сайт-игра для детей и подростков. Все данные о словах лежат в ОДНОМ файле `words.js`.
Чтобы добавить новый день с новыми словами, ИИ меняет ТОЛЬКО `words.js`. Логику, стили и разметку трогать НЕ нужно.

---

## 1. Структура проекта

| Файл | Назначение | Когда править |
|---|---|---|
| `index.html` | Разметка экранов (главная, неделя, игра), логотип, подвал, мета-теги для соцсетей | Не трогать при добавлении слов |
| `style.css` | Стили и 13 тем (группы по 4 недели) | Не трогать |
| `app.js` | Логика игры, прогресс, озвучка, открытие недель | Не трогать |
| `words.js` | **Все слова: 52 недели × 5 дней + темы дней** | **Править только здесь** |
| `logo.png`, `favicon.ico` | Логотип и значок вкладки | Не трогать |

---

## 2. Формат данных в `words.js`

```js
const WEEKS = [
  // === Неделя 1 ===
  {
    days: [
      [ ['fr1', 'перевод 1', '🟠'], ['fr2', 'перевод 2', '🟠'], ['fr3', 'перевод 3', '🟠'], ['fr4', 'перевод 4', '🟠'], ['fr5', 'перевод 5', '🟠'] ], // День 1
      [ /* 5 слов */ ], // День 2
      [ /* 5 слов */ ], // День 3
      [ /* 5 слов */ ], // День 4
      [] // День 5 — ВСЕГДА пустой, соберётся из дней 1–4 автоматически
    ],
    theme: [ 'Тема дня 1', 'Тема дня 2', 'Тема дня 3', 'Тема дня 4' ]
  },
  // ... всего 52 элемента
];
```

Каждое слово — массив из 3 элементов:
```js
['французское слово', 'перевод на русский', 'эмодзи']
```

### Поле `theme` (обязательное для заполненных недель)

У каждой заполненной недели (1–13) есть темы дней из книги — массив из **4 строк** (для дней 1–4):
```js
theme: [
  'Числа 1–5',
  'Фрукты',
  'Цвета',
  'В классе'
]
```
Тема показывается в карточке дня на экране недели («🎯 Тема: …»). При добавлении новой недели **обязательно** заполняй `theme` ровно 4 строками. День 5 (повторение) темы не имеет.

Пример реального слова из недели 11, дня 1:
```js
['jouer de la guitare', 'играть на гитаре', '🎸']
```

Пример заглушки, которую нужно ЗАМЕНИТЬ:
```js
['mot15-2-3', 'слово 15.2.3', '⭐']
```
Заглушка `mot<неделя>-<день>-<номер>`: `mot15-2-3` = неделя 15, день 2, слово 3.

---

## 3. Правила добавления слов (чек-лист)

1. Меняем **только** `words.js`.
2. В каждой неделе строго **5 дней**, в каждом из дней 1–4 строго **по 5 слов**.
3. День 5 (повторение) — **всегда пустой массив `[]`**. Слова для него тренажёр соберёт сам из дней 1–4 (20 слов, перемешанных).
4. В поле `fr` — **французское слово/фраза** в точном написании из книги (апострофы, дефисы, акценты сохраняем): `aujourd'hui`, `peut-être`, `écouter`.
5. В поле `ru` — короткий **перевод на русский**, понятный ребёнку.
6. Эмодзи — **обязательно**, один, тематический, соответствует слову (🎸 для гитары, 📷 для фото).
7. Обязательно добавь поле `theme` — ровно 4 строки (для дней 1–4), темы дней из книги.
8. Текст инструкций в игре уже на русском — менять его не нужно. Французский встречается только в словах.
9. Никакого HTML в полях — всё выводится как текст (безопасно).
10. Не создавать дубли слов внутри одного дня/недели.

---

## 4. Что учесть (тонкости тренажёра)

- **Диктант** сравнивает ответ без учёта регистра и **без учёта акцентов**:
  `é -> e`, `è -> e`, `ç -> c`, `œ -> oe` и т.д. Это сделано намеренно, чтобы детям было легче. Слова с акцентами можно добавлять спокойно.
- **Прописи**: фраза длиннее 12 символов автоматически разбивается на 2 строки (в canvas и в подписи). Canvas подстраивается под ширину экрана, а размер шрифта-подсказки уменьшается, чтобы слово влезло, — но всё равно не добавляй фразы длиннее ~22 символов.
- **Сопоставление**: перевод показывается вместе с эмодзи. Держи перевод коротким, чтобы кнопка пары не разрасталась.
- **Прогресс** хранится по ключу «неделя-день» (например `11-1`). Новый день с новыми словами автоматически получает свой прогресс — ничего настраивать не нужно.
- **Открытие недель**: на главной видны недели группами по 4. Следующая группа (5–8, 9–12 и т.д.) открывается, когда во **всех 4 неделях** текущей группы пройден **день 5 (повторение)**. Слова на это не влияют.
- **Статистика на главной**: показывается процент пройденных дней из всех 260 (52 недели × 5 дней). Считается автоматически.
- **Тема цвета** зависит только от номера недели (каждая группа из 4 недель — своя гамма). На слова не влияет.
- **Порядок слов**: экран приветствия и финал показывают слова **в том порядке, как они записаны в массиве** — поэтому записи для дней 1–4 держи в порядке книги (важно для дней с цифрами/порядковыми словами). Все задания (карточки, прописи, диктант, сопоставление) перемешивают слова сами.
- Слова берём из книги **«5 mots par jour»** и сохраняем её формулировки. Неделя 11, день 1 — эталонный образец (см. ниже).

---

## 5. Эталонный образец — недели 1–13

Недели 1–13 заполнены полностью, включая темы, — используй их как образец. Ниже — неделя 11.

```js
{
  days: [
    [
      ['chanter', 'петь', '🎤'],
      ['danser', 'танцевать', '💃'],
      ['jouer du piano', 'играть на пианино', '🎹'],
      ['jouer de la guitare', 'играть на гитаре', '🎸'],
      ['prendre une photo', 'делать фотографию', '📷']
    ],
    [
      ['propre', 'чистый', '🧼'],
      ['sale', 'грязный', '🗑️'],
      ['beau', 'красивый', '✨'],
      ['grand', 'большой', '🐕'],
      ['petit', 'маленький', '🐶']
    ],
    // день 3 (части лица) и день 4 (одежда) — тоже заполнены,
    // день 5 — пустой []
  ],
  theme: [
    'Действия / Хобби',
    'Прилагательные (описание собак)',
    'Части лица',
    'Одежда'
  ]
}
```

При добавлении новых дней ориентируйся на этот стиль: эмодзи + простое русское пояснение.

---

## 6. Проверка после правок

Обязательно прогнать проверки:

```bash
# 1. Синтаксис JS
node --check words.js

# 2. Структура данных: должно вывести 52 недели
node -e "const fs=require('fs');const s=fs.readFileSync('words.js','utf8').replace('const WEEKS =','globalThis.WEEKS =');eval(s);console.log('weeks:',WEEKS.length)"
```

Проверить вручную по каждой изменённой неделе:
- 5 дней в неделе;
- дни 1–4 по 5 слов;
- день 5 — пустой `[]`;
- в каждом слове 3 поля: французское, перевод, эмодзи;
- в `theme` — ровно 4 строки (дни 1–4).

Финал — открыть `index.html` в браузере и пройти изменённый день до конца:
карточки → прописи → диктант → сопоставление → финал. Убедиться, что на финале появляется звезда, а после 4 заданий день отмечается «Пройден».

---

## 7. Шпаргалка: французские акценты и спецсимволы

`é è ê ë` • `à â ä` • `ù û ü` • `î î ï` • `ô ö` • `ç` • `œ` (→ oe в диктанте) • `æ` (→ ae в диктанте)

Апостроф в словах типа `aujourd'hui` — пишем обычный `'`, без экранирования, кавычки вокруг слова двойные.

---

## 8. Чего НЕ делать

- Не менять `app.js`, `style.css`, `index.html` при добавлении слов.
- Не добавлять слова в другие файлы или в HTML.
- Не трогать день 5 (он автоматический).
- Не переименовывать `WEEKS` и не менять структуру «неделя → дни → слова».
- Не добавлять поле `theme` с неправильным количеством строк (должно быть ровно 4, для дней 1–4) и не вставлять темы в другие поля.
- Не удалять поле `theme` у уже заполненных недель (1–13).
- Не использовать нестандартные юникод-эмодзи-последовательности (ZWJ длиной более одного эмодзи) — могут плохо отображаться в canvas.
